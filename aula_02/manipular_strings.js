const nome = "Fulano"

console.log(nome.length)
console.log(nome + " de Freitas")
console.log(nome.includes("la"))

// Uso de crase para formatação de string
console.log(`Meu nome é ${42 + 5}`)

console.log(
    `Exibir
    Quebra
    de
    linha`
    )

// prompt('Qual o seu nome?')
// Rodar localmente
// docker compose run --rm app node aula_02/manipular_strings.js